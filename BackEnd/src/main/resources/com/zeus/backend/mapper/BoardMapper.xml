<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.backend.mapper.BoardMapper">
	 <!-- 공지사항 추가 -->
    <insert id="create">
        INSERT INTO NOTICE (
            no, user_id, user_name, user_email, user_nickname, image_url,
            category, subject, content, ref, step, depth, read_count, reg_date
        )
        VALUES (
            NOTICE_id_SEQ.NEXTVAL, #{userId}, #{userName}, #{userEmail}, #{userNickname}, #{imageUrl},
            #{category}, #{subject}, #{content}, #{ref}, #{step}, #{depth}, #{readCount}, #{regDate}
        )
    </insert>
    <!-- 공지사항 수정 -->
    <update id="modify">
        UPDATE NOTICE
        SET 
            user_id = #{userId},
            user_name = #{userName},
            user_email = #{userEmail},
            user_nickname = #{userNickname},
            image_url = #{imageUrl},
            category = #{category},
            subject = #{subject},
            content = #{content},
            ref = #{ref},
            step = #{step},
            depth = #{depth},
            read_count = #{readCount},
            reg_date = #{regDate}
        WHERE no = #{no}
    </update>
	<!-- 공지사항 상세 -->
	<select id="read" resultType="com.zeus.backend.domain.Notice">
		SELECT * FROM NOTICE WHERE no = #{no}
	</select>
	<!-- 목록 조회 -->
	<select id="list" resultType="com.zeus.backend.domain.Notice">
		SELECT * FROM NOTICE ORDER BY ref DESC, step ASC;
	</select>
	<!-- 공지사항 삭제 -->
	<delete id="delete"> 
		DELETE FROM NOTICE WHERE no = #{no}
	</delete>
	<!-- 조회수 증가 -->
	<update id="incrementReadCount">
		UPDATE NOTICE SET read_count = read_count + 1 WHERE no = #{no}
	</update>
</mapper>